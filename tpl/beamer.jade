!!!
html
  head
    title= title
    script(src="/jquery.min.js")
    script(src="/jquery.cycle2.min.js")
    script(src="/socket.io/socket.io.js")
    script!='var serverAddress = "' + server + '";'
    script(src="/beamer.js")
    link(rel="stylesheet", href="/styles.css")
    link(rel="stylesheet", href="/SM_beamer.css")
  body
    #main
      .cycle-slideshow(data-cycle-slides="> .cycle-slide", data-cycle-paused="true")
        each item in items
          .item.cycle-slide(href="#"+item.id, data-id=item.id, data-cycle-hash=item.id): img(src=item.img)